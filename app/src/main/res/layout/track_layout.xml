<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="61dp">

    <ImageView
        android:id="@+id/trackImage"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:layout_marginStart="13dp"
        android:layout_marginTop="8dp"
        android:src="@drawable/cover_rounded_corners"
        android:background="@drawable/not_find"/>

    <TextView
        android:id="@+id/trackName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="35dp"
        android:layout_marginStart="66dp"
        android:layout_marginTop="14dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:fontFamily="@font/ys_display_regular"
        android:textColor="?attr/colorPrimary"
        android:textSize="16sp"
        tools:text="Track Name" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="35dp"
        >

        <TextView
            android:id="@+id/groupName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="66dp"
            android:layout_marginTop="34dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:fontFamily="@font/ys_display_regular"
            android:textColor="?attr/colorPrimary"/>

        <ImageView
            android:layout_width="@dimen/track_dp"
            android:layout_height="@dimen/track_dp"
            android:layout_marginStart="@dimen/track_dp"
            android:layout_marginTop="@dimen/track_top"
            android:src="@drawable/searchdot"
            app:tint="?attr/colorPrimary"
            android:layout_weight="0"/>

        <TextView
            android:id="@+id/trackTime"
            android:layout_width="51dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/track_dp"
            android:layout_marginTop="@dimen/big_padding"
            android:fontFamily="@font/ys_display_regular"
            android:textColor="?attr/colorPrimary"
            tools:text="Time" />
    </LinearLayout>

    <ImageView
        android:layout_width="@dimen/track_8dp"
        android:layout_height="@dimen/track_14dp"
        android:layout_gravity="right"
        android:layout_marginTop="@dimen/track_25dp"
        android:layout_marginEnd="@dimen/track_20dp"
        android:src="@drawable/go"
        app:tint="?attr/colorPrimary" />
</FrameLayout>